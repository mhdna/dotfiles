super + shift + d
    dmenuhandlerXclip

super + shift + p
    passmenu

super + v
    clipmenu -i -l 15

super + shift + v
    st -e pulsemixer

super + grave
    dmenuunicode -i

super + shift + i
    emacs

super + i
    st -e my-tmux-session

super + alt + slash
    st -e transs

super + slash
    transXclip

super + shift + slash
    transXcclip

super + Insert
    bookmarkmenu

super + shift + Insert
    bookmarkit

super + w
    firefox

super + shift + w
    st -e nmtui

super + shift + b
    dmenu-bookmarks

super + e
    st -e lfub

super + shift + e
    st -e bicon -e neomutt

super + alt + m
    st -e bicon -e ncmpcpp

super + shift + r
    st -e bicon -e newsboatpkill

Print
    maim $XDG_PICTURES_DIR/screenshots/-$(date '+%y%m%d-%H%M-%S').png pic-full-

shift + Print
    maimpick

super + Print
    dmenurecord -i

super + Delete
    dmenurecord kill

# super + scroll_lock
#     pkill screenkey || screenkey


XF86Audio{RaiseVolume,LowerVolume,Mute}
	pamixer -{i,d,t} 2; kill -44 $(pidof dwmblocks)

super + {equal,minus,shift+minus}
	pamixer -{i,d,t} 2; kill -44 $(pidof dwmblocks)
# super + shift + equal
# 	pamixer --set-volume 100
# super + minus
# 	pamixer -d 5
# super + shift + minus
# 	pamixer -t
# brightness
super + comma
	brightnessctl set 2%-
super + shift + comma
	brightnessctl set 1%
super + period
	brightnessctl set +2%
super + shift + period
	brightnessctl set 100%

XF86MonBrightnessDown
	brightnessctl set 10%-

super + XF86MonBrightnessDown
	brightnessctl set 1%

XF86MonBrightnessUp
	brightnessctl set +10%

super + XF86MonBrightnessUp
	brightnessctl set 100%

ctrl + alt + Escape
    xkill

super + BackSpace
    dunstctl close-all

super + shift + BackSpace
    dunstctl history-pop

super + ctrl + BackSpace
    dusntctl set-paused toggle && notify-send "Toggled dunst"

super + F11 
    mpc toggle

super + shift + F11
    pauseallmpv

super + F10
    mpc seek -1%

super + shift + F10
    mpc prev

super + F12
    mpc seek +1%

super + shift + F12
    mpc next

super + F1
    kdeconnect-indicator ; kdeconnect-cli --refresh

super + shift + F1
    kill $(pgrep kdeconnect)

super + F2
    redshift -O 5000

super + shift + F2
    redshift -x 

super + F3
    torwrap

super + shift + F3 
    td-toggle    

super + F4
    dmenumount -i

super + shift + F4
    displayselect

super + F5
    remaps && notify-send 'Keyboard layout' 'Has been reset to default'

super + shift + q
	sysact

# make sxhkd reload its configuration files:
super + ctrl + r
	pkill -USR1 -x sxhkd 

#& bspc wm -r

##
## bspwm hotkeys
##
#
## quit/restart bspwm
## super + alt + {q,r}
## 	bspc {quit,wm -r}
#
## close and kill
## super + {_,shift + }w
#super + shift + c
#	bspc node -{c,k}
#
## alternate between the tiled and monocle layout
#super + m
#	bspc desktop -l next
#
## send the newest marked node to the newest preselected node
#super + y
#	bspc node newest.marked.local -n newest.!automatic.local
#
## swap the current node and the biggest window
#super + g
#	bspc node -s biggest.window
#
##
## state/flags
##
#
## set the window state
#super + {t,shift + t,s,f}
#	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}
#
##
## set the node flags
#super + ctrl + {m,x,y,z}
#	bspc node -g {marked,locked,sticky,private}
#
##
## focus/swap
## focus the node in the given direction
#super + {_,shift + }{h,j,k,l}
#	bspc node -{f,s} {west,south,north,east}
#
## focus the node for the given path jump
#super + {p,b,comma,period}
#	bspc node -f @{parent,brother,first,second}
#
## focus the next/previous window in the current desktop
## super + {_,shift + }c
## 	bspc node -f {next,prev}.local.!hidden.window
#
## focus the next/previous desktop in the current monitor
#super + bracket{left,right}
#	bspc desktop -f {prev,next}.local
#
## focus the last node/desktop
#super + {grave,Tab}
#	bspc {node,desktop} -f last
#
## focus the older or newer node in the focus history
#super + {o,i}
#	bspc wm -h off; \
#	bspc node {older,newer} -f; \
#	bspc wm -h on
#
## focus or send to the given desktop
#super + {_,shift + }{1-9,0}
#	bspc {desktop -f,node -d} '^{1-9,10}'
#
##
## preselect
##
#
## preselect the direction
#super + ctrl + {h,j,k,l}
#	bspc node -p {west,south,north,east}
#
## preselect the ratio
#super + ctrl + {1-9}
#	bspc node -o 0.{1-9}
#
## cancel the preselection for the focused node
#super + ctrl + space
#	bspc node -p cancel
#
## cancel the preselection for the focused desktop
#super + ctrl + shift + space
#	bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel
#
##
## move/resize
##
#
## expand a window by moving one of its side outward
#super + alt + {h,j,k,l}
#	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}
#
## contract a window by moving one of its side inward
#super + alt + shift + {h,j,k,l}
#	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}
#
## move a floating window
#super + {Left,Down,Up,Right}
#	bspc node -v {-20 0,0 20,0 -20,20 0}
