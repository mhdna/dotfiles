#!/bin/sh

while true
do
    mpc idle player > /dev/null 2>&1
    notify_title="Now Playing"
    music_info=$(mpc current -f "%file%\\n%artist% - %album%")
    notify-send -u low -t 5000 "$notify_title"  "$music_info"
done
