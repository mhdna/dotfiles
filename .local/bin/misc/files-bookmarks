#!/bin/sh

# ignore comments, blank lines, and whitespaces
sed -e 's/\s*#.*//' \
    -e '/^$/d' \
    -e 's/^\S*\s*//' \
    -e '.*\;s$' \
    ${XDG_CONFIG_HOME:-$HOME/.config}/shell/bm-dirs | fzf | sed 's|~|$HOME|'
