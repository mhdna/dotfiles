#!/bin/sh

pidof -x sb-keyboard >/dev/null 2>&1 || sb-keyboard -d >/dev/null 2>&1&

daemon(){
    xkbmon -u |
    while read -r line; do
        pkill -RTMIN+17 dwmblocks
    done
}

show(){
    xkbmon -u -o
}


case "$1" in
    "-d") daemon;;
    "-s") show;;
    "*") exit 1;;
esac
