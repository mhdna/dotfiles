#/bin/sh

ping -q -c 1 8.8.8.8 > /dev/null || exit

pgrep -f newsboat$ && exit 1

echo "N: Updating" > /tmp/newsupdate
pkill -RTMIN+3 ${STATUSBAR:-i3blocks}
/usr/bin/newsboat -x reload
rm -f /tmp/newsupdate
pkill -RTMIN+3 ${STATUSBAR:-i3blocks}
